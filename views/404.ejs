<%- include('partials/header') %>

<div class="flex flex-col items-center justify-center min-h-[60vh] text-center px-4">
    
    <div class="relative mb-8">
        <div class="absolute -inset-4 bg-pink-500/20 rounded-full blur-xl"></div>
        <div class="relative text-9xl font-black text-gray-800 select-none">
            404
        </div>
        <div class="absolute inset-0 flex items-center justify-center">
            <span class="text-6xl">🤔</span>
        </div>
    </div>

    <h1 class="text-3xl md:text-4xl font-bold text-white mb-4">
        Halaman Tidak Ditemukan
    </h1>
    
    <p class="text-gray-400 max-w-md mb-8 leading-relaxed">
        Maaf, video atau halaman yang Anda cari mungkin sudah dihapus, diubah namanya, atau sedang tidak tersedia untuk sementara waktu.
    </p>

    <div class="flex flex-wrap gap-4 justify-center">
        <a href="/" class="px-8 py-3 bg-pink-600 hover:bg-pink-700 text-white font-bold rounded-full transition transform hover:scale-105 shadow-lg shadow-pink-600/20 flex items-center gap-2">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/></svg>
            Kembali ke Beranda
        </a>
        
        <a href="javascript:history.back()" class="px-8 py-3 bg-gray-800 hover:bg-gray-700 text-gray-300 font-bold rounded-full transition border border-gray-700 flex items-center gap-2">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/></svg>
            Kembali
        </a>
    </div>

</div>

<% if (typeof videos !== 'undefined' && videos.length > 0) { %>
<div class="mt-16 border-t border-gray-800 pt-12">
    <h3 class="text-xl font-bold text-white mb-6 flex items-center gap-2">
        <span class="text-pink-500">Video</span> Rekomendasi
    </h3>
    
    <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
        <% videos.forEach(vid => { %>
        <div class="group bg-gray-900 rounded-lg overflow-hidden border border-gray-800 hover:border-pink-500/50 transition-all">
            <a href="/video/<%= vid.slug %>" class="block relative aspect-video overflow-hidden">
                <img src="<%= vid.thumbnail %>" class="w-full h-full object-cover group-hover:scale-110 transition duration-500">
                <div class="absolute bottom-1 right-1 bg-black/80 text-[10px] font-bold px-1.5 py-0.5 rounded text-white">
                    <%= formatDuration(vid.duration_sec) %>
                </div>
            </a>
            <div class="p-2">
                <h4 class="font-bold text-xs text-gray-300 group-hover:text-pink-400 line-clamp-2">
                    <%= vid.title %>
                </h4>
            </div>
        </div>
        <% }) %>
    </div>
</div>
<% } %>

<%- include('partials/footer') %>